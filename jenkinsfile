pipeline {
  agent any
  stages {
    stage ('checkout') {
      steps {
        echo 'Checking out code'
      }
    }
    stage ('Build') {
      steps {
        echo 'Building code'
       }
    }
    stage ('Test') {
      steps {
        echo 'Testing code'
      }
    }
  }
}
